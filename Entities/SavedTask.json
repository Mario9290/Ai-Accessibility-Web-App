{
  "name": "SavedTask",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Title of the saved task"
    },
    "description": {
      "type": "string",
      "description": "Description of what the task helps with"
    },
    "category": {
      "type": "string",
      "enum": [
        "settings",
        "apps",
        "communication",
        "photos",
        "general"
      ],
      "description": "Category of the task"
    },
    "steps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "instruction": {
            "type": "string"
          },
          "visual_description": {
            "type": "string"
          },
          "audio_text": {
            "type": "string"
          }
        }
      },
      "description": "Array of step-by-step instructions"
    },
    "completion_count": {
      "type": "integer",
      "default": 1,
      "description": "How many times user has completed this task"
    },
    "language": {
      "type": "string",
      "default": "en",
      "description": "Language the task was created in"
    }
  },
  "required": [
    "title",
    "description",
    "category",
    "steps"
  ]
}